# 보안 범위와 제보

이 프로젝트는 Codex 스킬·문서 템플릿·로컬 설치/점검 도구입니다.
계정 생성, 클라우드 접속, SQL 적용, 원격 push, 배포는 도우미가 실행하지 않습니다.
쉘 설정·Codex 승인 정책을 바꾸거나 관리자 권한을 요청하는 설치 스크립트를 포함하지 않습니다.

## 보호하는 범위

기존 파일 덮어쓰기 방지, 경로의 심볼릭 링크 거부, 문서상 필수 작업 누락 탐지,
일부 하드코딩 비밀 패턴·Git 추적 위험 파일 탐지를 제공합니다.
`check`는 실제 .env 값이나 private-data 내용을 열지 않습니다. 추적 파일 경로만 점검합니다.

## 보호하지 못하는 범위

스킬 지침은 실행기 수준의 강제 정책이 아닙니다. 모델이 지침을 항상 따르는지 보증하지 않습니다.
문서의 검증 근거가 사실인지 증명하지 않습니다. DB/API/로그인/서비스 운영을 자동 시험하지 않습니다.
전체 Git 이력, 모든 비밀 형식, 모든 개인정보, 모든 공격 경로를 탐지하지 않습니다.
큰 파일·빌드 결과·설치된 스킬·로컬 학생 데이터/백업 폴더는 내용 검사에서 제외될 수 있습니다.
실제 자료를 쓰기 전 학교의 외부 서비스·개인정보 처리 검토와 역할별 보안 시험이 필요합니다.

## 제보와 사고 대응

공개 이슈에 실제 학생 자료·토큰·비밀번호·접속 쿠키를 넣지 마세요.
저장소 관리자가 별도 비공개 제보 창구를 마련하면 이를 이용하고, 그렇지 않으면 비식별 재현 정보만 공유하세요.
비밀 노출이 의심되면 해당 키를 폐기·재발급하고 운영자에게 접근 이력/배포/저장소 이력을 점검하도록 알립니다.
GitHub에서 파일만 지우거나 .gitignore를 추가하는 것으로 끝내지 않습니다.
