interface:
  display_name: "School Vibe Builder (이전 이름)"
  short_description: "기존 호출을 DIVE 공통 하네스와 웹앱 모드로 연결"
  default_prompt: "$school-vibe-builder로 기존 프로젝트를 이어서 도와줘."
policy:
  allow_implicit_invocation: false
