# DIVE Builder 저장소 유지보수 규칙

이 저장소는 스킬 배포물이다. 여기에서 사용자 앱을 scaffold하거나 앱용 PROJECT/PLAN을 만들지 않는다.
공통 하네스(dive-builder)와 선택형 공유 웹앱(dive-webapp)을 분리한다. school-vibe-builder는 얇은 호환 진입점이다.
공통 모드에 교사/학생 역할, 특정 프로젝트 종류, 고정 웹 기술·단계·작업 ID를 강제하지 않는다.
스킬은 같은 프로젝트 문서를 재사용한다. 중복 상태 JSON이나 별도 개발 엔진을 만들지 않는다.

기존 파일·사용자 변경을 보존한다. 설치/초기화는 덮어쓰기·심볼릭 링크 쓰기를 거부한다.
Node 도우미는 의존성 없이 로컬에서 동작한다. 자동 원격 실행·클라우드 API·승인 해제를 추가하지 않는다.
웹앱의 적용 제외는 합의한 비적용 이유일 뿐이다. RLS/비밀 보호·필수 권한 시험을 우회하는 수단으로 만들지 않는다.
문서 점검을 실제 보안·권한·서비스 검증이라고 부르지 않는다.
변경 후 npm test, npm run verify를 실행하고, 실제로 하지 않은 Codex/브라우저/클라우드 검증을 통과라고 쓰지 않는다.
스킬 간 경로·독립 설치·이관·범용 프로젝트·기존 프로젝트 재개 사례를 함께 점검한다.
